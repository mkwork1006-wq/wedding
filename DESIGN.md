# デザイン設計書

## 1. 目的
- ハレの日を白を基調にした清潔感ある空間で表現し、余白・タイポグラフィ・やわらかなラインを通じて「静と動のリズム」を伝える。
- モバイルでも味わいを損なわないよう、余計な装飾は排しながら、セクションごとに質感のコントラストを付けて視線を誘導する。

## 2. ビジュアル指針
- 背景は純白で統一し、テキストとボーダーにグレー〜黒の階調を使うことで洗練された印象。
- アクセントにはしっとりとした赤紫（#a1125a）を置き、ボタンやラベルで品のある彩度を差す。
- 影やボーダーでレイヤー感を演出しつつ、全体的に「軽さ」を意識したストローク（線）を使う。

## 3. ページ構成
1. ヘッダー：ブランド名と日付、右端にハンバーガーメニュー。デスクトップではナビリンクを明示、モバイルではボタンのみ。
2. ヒーロー：大きな見出し＋本文＋アクション。白背景にボタンと小さな彩度差を置いたカードで分節。
3. ストーリー：3つのカードでポイントを表現。背景は白のまま、カードに軽いシャドウと境界のグラデーションを追加。
4. 詳細・タイムライン：グリッドで構成し、カードにボーダーと余白で静的なリズムを付与。中身は一文の説明＋タグで統一。
5. RSVP：ボタン付きのパネル。リストは丸いインジケーターでアクセント。

## 4. ハンバーガーメニューの仕様（例：「125naroom」のような動き）
- トグルボタンは3本線で構成し、クリックすると中央ラインが消えて上下が十字に変形するアニメーション。
- 背景のオーバーレイは淡い白で背面を明るく保ちつつ、クリックで閉じられるよう設定。
- 出現するメニューカードは白ベースで、角丸 + わずかなシャドウ。リンクを縦に並べ、クリックでスムーズに閉じる。
- この動きを SCSS ではなく Tailwind の `transition`, `rotate`, `opacity` を組み合わせて実現。

## 5. アニメーション/インタラクション
- メニューの開閉は `transition duration-300` で線の回転とフェードを表現。
- カードやリンクには `hover:-translate-y-1` や `hover:shadow` で静かな動きを添えて、全体のリズムを整える。

## 6. 今後の拡張候補
1. 写真挿入セクションを追加し、背景に微細な粒子/グラデーションを入れる。
2. RSVP ボタンにモーダルやフォームへの導線を接続。
3. アクセシビリティ強化として `prefers-reduced-motion` 対応を追加。
