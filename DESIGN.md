# デザイン設計書

## 1. 目的と全体像
- 「Tomotomo Wedding」が白と柔らかな余白で包まれたウェブ体験を持つこと。多くのスクロールを誘発せず、各ページ（トップ・席次表・コースメニュー・新郎プロフィール・新婦プロフィール・ギャラリー・Q&A）を明確に分けて順に案内する構成にしています。
- トップページでは概念的なコンセプトとミニマルな誘導に留め、他のページはそれぞれ単独で展開する（ファイル切り分け済み）想定です。

## 2. ナビゲーションとハンバーガーメニュー
- ヘッダーは `sticky top-0` でページ上部に固定し、背景を半透明の白にしてスクロールでも目立たず、ボタン配置はフレックスで整理。デスクトップではボタン、モバイルでは3本線のアイコンを表示し、開閉時には上段が +45°、下段が −45° に回転してクロスになるアニメーションを加えています。
- モバイルのメニューは `MenuPanel` でヘッダー直下に展開し、背景色をページと同じ白に設定することで上下のレイヤーを自然につなげています。リンクを押すとパネルを閉じて関連ページ（アプリ内では Section を切り替え）に遷移します。グレーのボーダーと淡いシャドウでパネルを区切り、125naroom のような動きを模倣しながらもカラーは清潔感を保ちます。

## 3. 各ページ（セクション）の構成
1. **トップ (TopPage)**：トップ画像フォルダ（`src/assets/images/top`）の `TOP_*` 画像からランダムで1枚を選び、フルブリード（左右の余白なし）で表示します。上に英語コピーを白のゴシック体で重ね、写真の上下に薄い白い影は載せません。画像は初回表示で100%から2秒かけて105%へ緩やかにズームし、拡大の基準は下側（脚が見切れにくいように）です。表示位置は全体で25px下げ、`TOP_B.jpg` のみ追加で50px下げます。下段に大きめのヘッダー文字とリード文、サブカードで「記憶のモチーフ」を表現します。
2. **席次表 (SeatingPage)**：受付から親族席までの動線をリストで示し、それぞれを丸みのあるカードで区切ります。各カードは薄いグレーのボーダーを持ち、背景は純白。
3. **コースメニュー (CoursesPage)**：6皿構成を 2 カラムで展開し、プレート名を簡潔に置いて背景のコントラストを出します。カードには微細なシャドウとグラデーションを採用。
4. **新郎プロフィール (GroomPage)**：箇条書きの詳細をリストで提示し、カード全体に余白を与えて読みやすさを確保。
5. **新婦プロフィール (BridePage)**：新郎ページと同様のカード構成で、二人それぞれの語りを別ページとしてまとめることで深掘りできます。
6. **ギャラリー (GalleryPage)**：3 枚のカードに光のグラデーション背景を敷き、タイトルと解説で雰囲気を伝える。
7. **Q&A (FaqPage)**：よくある質問を縦に並べたカードで収め、親しみやすい文体で回答します。

## 4. カラーと質感
- 背景: `#ffffff` をベースに、セクションパネルは `#fafafa` や `#efeded` でトーンを添える。テキストは `#1f1f1f` から `#4c4c4c` の濃淡、アクセントには `#a1125a` を使用。トップの英語コピーは白で写真の上に載せます。
- メニュー: 背景も白、ボーダーは `#f0f0f0`、文字は黒で統一。開閉時にショドウを `shadow-[0_20px_60px_rgba(0,0,0,0.08)]` にして浮き上がり感を演出。
- カード: 全体的に角丸 `rounded-[24px]` 以上で統一し、微かなシャドウとグラデーションを落として優雅な重なりを表現。

## 5. インタラクション
- ハンバーガーアイコンは `transition duration-300` に `translate`/`rotate` を組み合わせて 125naroom 風のクロスアニメーション。`MenuPanel` は `max-height` をトグルし、展開時に `py-6` を加えてスライド感を演出。
- カードには `hover:-translate-y-1` や `hover:shadow-lg` を適用し、それぞれに静かな反応を持たせる。
- トップ画像は `scale(1)` から `scale(1.05)` へのズームアニメーション（2秒）を付与し、`prefers-reduced-motion` では無効化します。

## 6. 今後の展開
1. 現在は `App.jsx` 内で `activePage` を切り替えているが、各リンクごとに `src/pages` から個別のルート (`/seating`, `/courses` …) を用意することでより堅牢な多ページ構成に移行可能。
2. メニュー展開時に `backdrop-blur` を部分的に足すことで、背景を淡くマスクしフォーカスを集中させるアレンジも検討。
3. FAQ やギャラリーに API 連携や CMS 連携を加え、更新性の高いコンテンツ運用に拡張。
