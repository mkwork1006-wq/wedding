# デザイン設計書

## 1. 目的とトーン
- 主役の「Tomotomo Wedding」を白を基調とした清潔感と余白で包み込み、ゲストが自然体で祝福に向き合える空気を届ける。
- 各セクション（トップ・席次表・コースメニュー・新郎／新婦プロフィール・ギャラリー・Q&A）は別ページとして構築される想定で、トップページでは誘引だけを行い、ゆったりとした間とテキストを活かす。

## 2. ナビゲーションとメニュー
- ヘッダーは `sticky top-0` でスクロールに追従し、右端にはテキストや枠のない3本線アイコンを対照的に配置。開くとヘッダー直下で背景と同じ白いパネルが展開し、縦にリンクを並べる。
- モーダル的なオーバーレイではなく、125naroom のように上部からスライドするような上下の回転アニメーションを持ったハンバーガーアイコンで、開閉時に上段が +45°、下段が -45°に交差し、中央ラインがフェードする演出。
- パネル内のリンクは `href` でそれぞれのページ（#seating など）に遷移し、タップ後はパネルを閉じて対応セクションへフォーカス。必要に応じて将来的には別ファイルへの遷移も可能な構成。

## 3. セクション構成
1. **ヒーロー（トップ）**：大判タイポ＋本文で簡潔にコンセプト提示。サブカードで「記憶のメモリー」を演出し、入り口の空気感を伝える。
2. **席次表セクション**：受付から親族席までをシンプルなカードリストでまとめ、ゲストの動線・区分を明快に提示。
3. **コースメニュー**：6皿構成をカードで並べ、グラデーションのある背景や影で視覚のリズムを保つ。
4. **新郎／新婦プロフィール**：それぞれ独立したカードで人物像や想いを箇条書きにし、Landing ページながら別ページ感を持たせる。
5. **ギャラリー**：3つのビジュアルカードで光の質感を表現し、次のページへ誘導する入口として機能。
6. **Q&A**：よくある問いと回答を丸い角のカードで整えることで安心感を添える。

## 4. カラー/マテリアル
- 背景はピュアホワイト、テキストはダークグレー～ブラック、アクセントはボルドー寄りの赤紫（#a1125a）とし、影やボーダーでレイヤー感を出す。
- メニューは背景を白で統一し、文字は黒。ボーダーに薄いグレーを使い、開閉時のフィード感を強調するために `shadow-[0_20px_60px_rgba(0,0,0,0.08)]` を用いる。

## 5. 動的挙動
- ハンバーガーアイコンは Tailwind の `transition duration-300`、`rotate`、`translate` を組み合わせて 125naroom 風の変化を再現。`MenuPanel` は `max-height` を切り替えてスライドさせ、シャドウ・padding・英文テキストで区切る。
- カードには `hover:-translate-y-1` や `hover:shadow-lg` を適用し、静かに反応する体験を提供。

## 6. 今後の展開
1. それぞれのナビリンクに対し、`/seating` などの別ファイルを用意し、データや画像をリッチに差し替える。
2. メニュー展開時に背景を少しマスクする `backdrop-blur` を導入してフォーカスを集中させるオプション。
3. RSVP や予約フォームへの導線をボトムに追加することで、行動喚起の強化。
