# Tomotomo Wedding 統合設計書

最終更新: 2026-02-23

## 1. 文書の目的
- これまでの制作指示と実装方針を一つに集約し、以後の改修時に判断がぶれない状態を維持する。
- デザイン方針だけでなく、言語運用、技術選定、ページ構成、将来の拡張前提を明文化する。

## 2. 基本方針（運用ルール）
- すべての説明文、UI 文言、コードコメントは日本語で記述する。
- 実装は React を第一選択とし、スタイルは Tailwind CSS を使用する。
- 単体モックを作る場合は原則として 1 ファイル完結（React は `.jsx`、静的は `.html`）とする。
- トーンは「上品・軽やか・清潔感」を軸にし、過剰装飾より余白と質感を重視する。
- レイアウトはモバイルファーストで設計し、軽いトランジションやアニメーションを含める。

## 3. 情報設計・ページ構成
- 想定ページは `TopPage` / `SeatingPage` / `CoursesPage` / `GroomPage` / `BridePage` / `GalleryPage` / `FaqPage`。
- 現在の実装では `activePage` で画面を切り替える SPA 構成を採用している。
- 現在の `ProfilePage` は新郎・新婦を 1 画面内スライドで切り替える実装だが、運用前提は将来的なページ分離（`/groom`、`/bride`）とする。
- ヘッダーのナビゲーションまたはモバイルメニューから、各ページへ明確に遷移できる構成を維持する。

## 4. UI デザイン指針
- ベースカラーは白（`#ffffff`）で、薄いグレーの境界線や面で階層を作る。
- 文字色は可読性重視で濃淡を分け、アクセントカラーは限定的に使用する。
- カード UI は角丸と薄いシャドウで統一し、清潔で柔らかな印象を保つ。
- 背景は単色で終わらせず、必要に応じて繊細なグラデーションや光表現を加える。
- PC とモバイルの両方で破綻しない余白・行間・タップ領域を確保する。

## 5. インタラクション指針
- メニュー開閉、ページ切り替え、カードホバーには短く滑らかな `transition` を使う。
- 動的メニューまたはセクション切り替えを必ず含め、情報移動を視覚的に理解できるようにする。
- 動きは演出過多にせず、コンテンツの読みやすさを妨げない範囲に留める。
- `prefers-reduced-motion` を尊重し、必要に応じてアニメーションを抑制する。

## 6. コンテンツ設計（現行実装準拠）
- トップ: キービジュアルを主役にし、結婚式全体のトーンを提示する。
- 席次表: ゲスト導線や配置意図を分かりやすく提示する。
- コース: 提供順が直感的に伝わるカード構成で見せる。
- プロフィール: 新郎新婦それぞれの人物像を同等の密度で表現する。
- ギャラリー: 写真中心で空気感を見せる。
- FAQ: 参列前の不安を短文で解消する。

## 7. 実装アーキテクチャ方針
- 当面は `src/App.jsx` の `activePage` 制御で運用し、UI 検証を優先する。
- 実運用時は各ページをルーティング分離し、URL 直アクセスを可能にする。
- 分離時もデザインルール（白基調、軽やかな遷移、統一ナビ）を維持する。
- データ定義は `src/data/content.js` のような集中管理を基本とし、表示コンポーネントから分離する。

## 8. 保守ルール
- 方針変更があった場合はこの `DESIGN.md` を最優先で更新する。
- 大きな UI 変更時は、変更意図と影響範囲（対象ページ・挙動・文言）を追記する。
- 将来のページ分離や CMS 連携を見据え、コンポーネントの責務分離を維持する。
